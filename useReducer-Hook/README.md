##  What I Learned — useReducer

- "useReduce" Hook
- Initial state
- Reducer function
- Actions & action types
- "dispatch()"
- Action "payload"
- "switch" statement for handling actions
- Updating state immutably
- Using "map()", "filter()", "findIndex()" and "reduce()" with reducer logic
- Managing multiple related state values with one reducer
- Handling complex state updates in a structured way
- Understanding the flow: Action → Dispatch → Reducer → New State → UI Update